import org.gradle.internal.xml.XmlTransformer

apply plugin: 'idea'

subprojects {
	apply plugin: 'idea'
}

tasks.register('ideacodestyle', Copy) {
	from file('config/idea')
	into file('.idea')
	include '**/*.xml'
}